## 📘 Lesson 14: Real-World Mini Project — React Dashboard App

---

### 🎯 Project Goal

Build a small dashboard that includes:

✅ Pages:

* `/dashboard` – shows welcome and user info
* `/users` – list of users with details
* `/settings` – toggle app theme (dark/light)

✅ Features:

* Uses **React Router**
* Uses **Context + useReducer** for global state
* Controlled form to **add new users**
* Optimized components with `React.memo`, `useCallback`

---

## 🏗️ Project Setup

If you don’t already have it:

```bash
npm create vite@latest react-dashboard --template react
cd react-dashboard
npm install
npm install react-router-dom
```

---

### 🗂 Folder Structure

```
src/
├── App.jsx
├── main.jsx
├── context/AppContext.jsx
├── pages/
│   ├── Dashboard.jsx
│   ├── Users.jsx
│   └── Settings.jsx
├── components/
│   ├── UserForm.jsx
│   ├── UserList.jsx
```

---

## 🧩 Step-by-Step Implementation

---

### ✅ Step 1: Create Context + Reducer

📄 `context/AppContext.jsx`

```jsx
import { createContext, useReducer } from 'react';

const initialState = {
  users: [],
  theme: 'light'
};

function reducer(state, action) {
  switch (action.type) {
    case 'ADD_USER':
      return { ...state, users: [...state.users, action.payload] };
    case 'TOGGLE_THEME':
      return { ...state, theme: state.theme === 'light' ? 'dark' : 'light' };
    default:
      return state;
  }
}

export const AppContext = createContext();

export function AppProvider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState);
  return (
    <AppContext.Provider value={{ state, dispatch }}>
      <div className={state.theme}>{children}</div>
    </AppContext.Provider>
  );
}
```

---

### ✅ Step 2: Setup Router in App

📄 `App.jsx`

```jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';
import Dashboard from './pages/Dashboard';
import Users from './pages/Users';
import Settings from './pages/Settings';

export default function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/dashboard">Dashboard</Link> |{' '}
        <Link to="/users">Users</Link> |{' '}
        <Link to="/settings">Settings</Link>
      </nav>

      <Routes>
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/users" element={<Users />} />
        <Route path="/settings" element={<Settings />} />
      </Routes>
    </BrowserRouter>
  );
}
```

📄 `main.jsx`

```jsx
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
import { AppProvider } from './context/AppContext.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <AppProvider>
    <App />
  </AppProvider>
);
```

---

### ✅ Step 3: Create Pages

📄 `pages/Dashboard.jsx`

```jsx
import { useContext } from 'react';
import { AppContext } from '../context/AppContext';

export default function Dashboard() {
  const { state } = useContext(AppContext);
  return (
    <div>
      <h2>🏠 Dashboard</h2>
      <p>Total Users: {state.users.length}</p>
      <p>Current Theme: {state.theme}</p>
    </div>
  );
}
```

---

### ✅ Step 4: Users Page with Form + List

📄 `pages/Users.jsx`

```jsx
import UserForm from '../components/UserForm';
import UserList from '../components/UserList';

export default function Users() {
  return (
    <div>
      <h2>👥 Users</h2>
      <UserForm />
      <UserList />
    </div>
  );
}
```

---

📄 `components/UserForm.jsx`

```jsx
import { useState, useContext } from 'react';
import { AppContext } from '../context/AppContext';

export default function UserForm() {
  const { dispatch } = useContext(AppContext);
  const [name, setName] = useState('');

  function handleSubmit(e) {
    e.preventDefault();
    if (name.trim()) {
      dispatch({ type: 'ADD_USER', payload: { id: Date.now(), name } });
      setName('');
    }
  }

  return (
    <form onSubmit={handleSubmit}>
      <input
        placeholder="User Name"
        value={name}
        onChange={(e) => setName(e.target.value)}
      />
      <button type="submit">Add User</button>
    </form>
  );
}
```

---

📄 `components/UserList.jsx`

```jsx
import { useContext, memo } from 'react';
import { AppContext } from '../context/AppContext';

const UserList = memo(() => {
  const { state } = useContext(AppContext);
  return (
    <ul>
      {state.users.map((user) => (
        <li key={user.id}>👤 {user.name}</li>
      ))}
    </ul>
  );
});

export default UserList;
```

---

### ✅ Step 5: Settings Page – Toggle Theme

📄 `pages/Settings.jsx`

```jsx
import { useContext } from 'react';
import { AppContext } from '../context/AppContext';

export default function Settings() {
  const { state, dispatch } = useContext(AppContext);
  return (
    <div>
      <h2>⚙️ Settings</h2>
      <p>Current Theme: {state.theme}</p>
      <button onClick={() => dispatch({ type: 'TOGGLE_THEME' })}>
        Toggle Theme
      </button>
    </div>
  );
}
```

---

### 🎨 Optional: Add Simple Theme Styles

📄 `index.css` or `App.css`

```css
.light {
  background: #f9f9f9;
  color: #222;
}
.dark {
  background: #222;
  color: #f9f9f9;
}
```

---

## ✅ What You've Practiced

| Concept               | Used In                      |
| --------------------- | ---------------------------- |
| Routing               | `react-router-dom` in App    |
| Context + useReducer  | Global state for theme/users |
| Controlled form       | `UserForm.jsx`               |
| Memoized component    | `UserList` with `React.memo` |
| Component composition | Reusing UI in pages          |

---

### 🧪 Exercise 10: Optional Extensions

✅ Try adding:

* Delete user button per user
* Edit user (inline or popup)
* Save users to localStorage
* Custom theme selector with dropdown

---

## 🎯 What’s Next?

👉 **Lesson 15: Hooks Deep Dive – Custom Hooks, useRef, useLayoutEffect, useImperativeHandle**

You’ll learn:

* How to build your **own hooks**
* How to access and control **DOM elements**
* When to use advanced lifecycle hooks
